"""Placeholder experiment entrypoint.

The workflow's agents and skills reference `scripts/generate_wrong_answers.py` as the
project's experiment entrypoint. Replace this stub with your own
implementation (the source project used Hydra-configured training /
eval pipelines here).
"""

import sys

sys.exit("scripts/generate_wrong_answers.py is a placeholder — wire up your project's entrypoint.")
